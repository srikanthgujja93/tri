public class sqare {
    public double length{get; set;}
    public double CalculateArea(double length )
    {
          double Areaofsqare =math.pow(length, 2);
            system.debug('length of sqare is ' + length);
        return Areaofsqare;
        
    } 

}